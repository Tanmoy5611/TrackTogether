package TrackTogether.domain;

public enum TravelGroupActivityType {
    CREATED,
    JOINED,
    LEFT,
    UPDATED,
    OWNERSHIP_TRANSFERRED,
    JOIN_REQUESTED,
    JOIN_REQUEST_ACCEPTED,
    JOIN_REQUEST_REJECTED
}